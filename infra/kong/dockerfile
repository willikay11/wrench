# infra/kong/Dockerfile

FROM kong:3.6-ubuntu

# Copy declarative config
COPY kong.yaml /kong/declarative/kong.yaml

# Set environment variables
ENV KONG_DATABASE=off
ENV KONG_DECLARATIVE_CONFIG=/kong/declarative/kong.yaml
# Default PORT for local builds; Railway overrides this at runtime
ENV PORT=8000
ENV KONG_ADMIN_LISTEN=0.0.0.0:8001
ENV KONG_LOG_LEVEL=info
ENV KONG_PROXY_ACCESS_LOG=/dev/stdout
ENV KONG_ADMIN_ACCESS_LOG=/dev/stdout
ENV KONG_PROXY_ERROR_LOG=/dev/stderr
ENV KONG_ADMIN_ERROR_LOG=/dev/stderr

# Validate config on build
RUN kong config parse /kong/declarative/kong.yaml

EXPOSE 8000 8001

# Use shell form so ${PORT} expands at container start from Railway's injected PORT
CMD ["sh", "-c", "KONG_PROXY_LISTEN=0.0.0.0:${PORT} kong start --vv"]